#!/usr/bin/env node

import { execSync } from "child_process";

const args = process.argv.slice(2);
const command = `npx -y @prismarine/cli@latest ${args.join(" ")}`;
execSync(command, { stdio: "inherit" });
